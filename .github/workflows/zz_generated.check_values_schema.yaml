# DO NOT EDIT. Generated with:
#
#    devctl
#
#    https://github.com/giantswarm/devctl/blob/d0c8f03c86ad4cfaef54d2315dc6f6d402be716d/pkg/gen/input/workflows/internal/file/check_values_schema.yaml.template
#

name: 'Values and schema'

on:
  pull_request:
    branches:
      - master
      - main
    paths:
      - 'helm/**/values.yaml'         # default helm chart values
      - 'helm/**/values.schema.json'  # schema
      - 'helm/**/ci/ci-values.yaml'   # overrides for CI (can contain required entries)

permissions: {}

jobs:
  check:
    uses: giantswarm/github-workflows/.github/workflows/chart-values.yaml@main
    permissions:
      contents: read
